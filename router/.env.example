# Keep this file as a template. Put actual values only in an untracked .env file.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# Optional: bearer key required by clients of the local router.
ROUTER_BEARER_TOKEN=
